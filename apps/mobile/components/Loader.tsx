import { Text, View } from "react-native";

export const Loader = () => {
  return (
    <View className="flex h-[100vh] flex-col items-center justify-center">
      <Text>Loading...</Text>
    </View>
  );
};
